# ==========================================================================
# CRE SECRETS FILE
# ==========================================================================
# This workflow doesn't use runtime.getSecret() - the confidential input it
# processes is the agent's transaction data (fetched via a confidential HTTP
# JSON-RPC call) and the verification criteria compared against it, not a
# Vault DON secret. See verify-task/workflow.ts.
secretsNames: {}
